# Required by next-iron-session (must be at least 32 characters)
SESSION_PASSWORD=supersecret_cookie_key_1234567890!

# Your Laravel backend is expected to run on port 3000 (as you mentioned before)
NEXT_PUBLIC_API_URL=http://localhost:3000
